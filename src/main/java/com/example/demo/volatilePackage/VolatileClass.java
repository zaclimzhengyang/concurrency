package com.example.demo.volatilePackage;

public class VolatileClass {
    /**
     * when variables declared with the `volatile` keyword, it is guaranteed that any
     * thread that reads the field will see the most recently written value
     * the volatile keyword will not perform any mutual exclusive lock on the variable
     */
}
